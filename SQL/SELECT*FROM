-- 全てのデータを取り出す
SELECT * FROM users;

-- 一部の行だけ取得する
SELECT * FROM userss WHERE age >= 20;

-- 複数の条件を組み合わせる(<> means not equal)
SELECT * FROM users WHERE age >= 20 AND user_id <> 6; 

--条件指定とカラム選択を組み合わせる
SELECT name, age FROM users WHERE age >= 20;
